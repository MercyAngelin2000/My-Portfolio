services:
  - type: web
    name: My-Portfolio
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
```

---

### Most Likely Fix Right Now:
Go to Render → **Settings** → change your build command to:
```
npm install && npm run build